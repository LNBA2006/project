import styled from 'styled-components';

import { COLOR } from '../../../../constants'

export const Done = styled.div`
  .ant-result-icon {
    margin-bottom: 0px;
    font-size: 64px;
    color: ${COLOR.blue4}
  }
  button {
    border-radius: 8px;
  }
`